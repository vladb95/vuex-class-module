import { Action, extractVuexModule, Getter, HasGetter, Mutation, VuexClass } from './vuex-class-module';

export default {
  VuexClass,
  Getter,
  Mutation,
  Action,
  HasGetter,
  extractVuexModule,
}